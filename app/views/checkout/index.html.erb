<table>
  <tr>
    <th></th>
    <th></th>
  </tr>
  <% @pledges.each do |pledge| %>
    <tr>
      <%= form_tag charge_pledge_path do %>
        <td><%= "#{pledge.campaign.name}" %></td>
        <%= hidden_field_tag 'pledge_id', pledge.id %>
        <%= hidden_field_tag 'pledge_id', pledge.id %>
        <td><%= submit_tag pledge.amount.to_s %></td>
      <% end %>
    </tr>
  <% end %>
</table>